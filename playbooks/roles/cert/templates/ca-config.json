{
    "signing": {
        "default": {
            "expiry": "{{ EXPIRE_TIME }}"
        },
        "profiles": {
            "kubernetes": {
                "expiry": "{{ EXPIRE_TIME }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth",
		    		"client auth"
                ]
            }
        }
    }
}